"""
Discord UI views for ridebot.

This package is not a cog package, so it is never auto-loaded by
`load_extensions`. Views defined here are instantiated and attached to
messages explicitly, and persistent views must be re-registered with
`bot.add_view(...)` from a cog's `cog_load`.
"""
