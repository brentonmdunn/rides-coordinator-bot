# Jobs

Scheduled jobs to be run by the bot at certain intervals. The logic for each job should be defined here and then added to the job scheduler in `../cogs/job_scheduler.py`.

> ⚠️ Jobs in `../jobs_disabled/` are currently inactive or in testing.
